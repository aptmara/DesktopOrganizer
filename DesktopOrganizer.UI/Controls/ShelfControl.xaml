<UserControl x:Class="DesktopOrganizer.UI.Controls.ShelfControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:DesktopOrganizer.UI.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200"
             AllowDrop="True" Drop="UserControl_Drop">
    <UserControl.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="4" BorderThickness="1" BorderBrush="Transparent"
                                AllowDrop="True"
                                PreviewMouseLeftButtonDown="ListBoxItem_PreviewMouseLeftButtonDown"
                                PreviewMouseMove="ListBoxItem_PreviewMouseMove"
                                Drop="ListBoxItem_Drop">
                            <ContentPresenter />
                            <Border.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Open" Click="MenuItem_Open_Click"/>
                                    <Separator/>
                                    <MenuItem Header="Remove" Click="MenuItem_Remove_Click"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#44FFFFFF"/>
                                <Setter Property="Panel.ZIndex" Value="1"/>
                                <EventSetter Event="MouseEnter" Handler="Item_MouseEnter"/>
                                <EventSetter Event="MouseLeave" Handler="Item_MouseLeave"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.1" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.1" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
        <local:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter"/>
    </UserControl.Resources>
    
    <Border CornerRadius="12" BorderBrush="#33FFFFFF" BorderThickness="1">
        <Border.Background>
            <SolidColorBrush Color="#CC1E1E24"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Direction="270" Opacity="0.5"/>
        </Border.Effect>
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Rename Shelf" Click="MenuItem_RenameShelf_Click"/>
                <Separator/>
                <MenuItem Header="Delete Shelf" Click="MenuItem_DeleteShelf_Click"/>
            </ContextMenu>
        </Border.ContextMenu>
        <Border.RenderTransform>
            <TranslateTransform Y="0"/>
        </Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.0" To="1.0" Duration="0:0:0.4"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.4">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border CornerRadius="11,11,0,0" Padding="10,6">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#66000000" Offset="0"/>
                        <GradientStop Color="#00000000" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="{Binding Title}" Foreground="#EEEEEE" FontWeight="SemiBold" 
                           VerticalAlignment="Center" FontSize="14"
                           TextTrimming="CharacterEllipsis"/>
            </Border>
            
            <!-- Items Area -->
            <ListBox Grid.Row="1" ItemsSource="{Binding Items}" 
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="70" Height="86" Margin="4" Opacity="{Binding IsBroken, Converter={StaticResource BooleanToOpacityConverter}}">
                            <Grid Width="48" Height="48" Margin="0,4,0,4">
                                <Image Source="{Binding Icon}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Grid>
                            <TextBlock Text="{Binding Title}" Foreground="#DDDDDD" 
                                       FontSize="11" TextWrapping="Wrap" TextAlignment="Center"
                                       TextTrimming="CharacterEllipsis" MaxHeight="30" LineHeight="14"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
