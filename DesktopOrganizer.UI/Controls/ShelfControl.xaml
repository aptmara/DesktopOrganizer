<UserControl x:Class="DesktopOrganizer.UI.Controls.ShelfControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:DesktopOrganizer.UI.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200"
             AllowDrop="True" Drop="UserControl_Drop">
    <UserControl.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" Padding="4">
                            <ContentPresenter />
                            <Border.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Open" Click="MenuItem_Open_Click"/>
                                    <Separator/>
                                    <MenuItem Header="Remove" Click="MenuItem_Remove_Click"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <local:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter"/>
    </UserControl.Resources>
    
    <Border Background="#CC202020" CornerRadius="8" BorderBrush="#44FFFFFF" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Background="#40000000" CornerRadius="8,8,0,0" Padding="8,4">
                <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="SemiBold" 
                           VerticalAlignment="Center"/>
            </Border>
            
            <!-- Items Area -->
            <ListBox Grid.Row="1" ItemsSource="{Binding Items}" 
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="64" Height="80" Margin="4" Opacity="{Binding IsBroken, Converter={StaticResource BooleanToOpacityConverter}}">
                            <Image Source="{Binding Icon}" Width="32" Height="32" Margin="0,4"/>
                            <TextBlock Text="{Binding Title}" Foreground="White" 
                                       FontSize="11" TextWrapping="Wrap" TextAlignment="Center"
                                       TextTrimming="CharacterEllipsis" MaxHeight="34"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
