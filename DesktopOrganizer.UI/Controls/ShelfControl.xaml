<UserControl x:Class="DesktopOrganizer.UI.Controls.ShelfControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:DesktopOrganizer.UI.Controls"
             xmlns:behaviors="clr-namespace:DesktopOrganizer.UI.Behaviors"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200"
             AllowDrop="True" Drop="UserControl_Drop">
    <UserControl.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="4" BorderThickness="1" BorderBrush="Transparent"
                                AllowDrop="True"
                                PreviewMouseLeftButtonDown="ListBoxItem_PreviewMouseLeftButtonDown"
                                PreviewMouseMove="ListBoxItem_PreviewMouseMove"
                                Drop="ListBoxItem_Drop">
                            <ContentPresenter />
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick" Command="{x:Static local:ShelfControl.OpenItemCommand}" CommandParameter="{Binding}" />
                            </Border.InputBindings>
                            <Border.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="開く" Click="MenuItem_Open_Click"/>
                                    <Separator/>
                                    <MenuItem Header="名前変更" Click="MenuItem_RenameItem_Click"/>
                                    <MenuItem Header="削除" Click="MenuItem_Remove_Click"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverOverlayBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                <Setter Property="Panel.ZIndex" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.1" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.1" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.1"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <local:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Border x:Name="RootBorder" CornerRadius="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{Binding BackgroundBrush}">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Opacity" Value="1.0"/>
                <Style.Triggers>
                    <!-- ゴーストモード: マウスが離れている時に半透明化 -->
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsGhostModeEnabled}" Value="True"/>
                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="False"/>
                        </MultiDataTrigger.Conditions>
                        <MultiDataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.25" Duration="0:0:0.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </MultiDataTrigger.EnterActions>
                        <MultiDataTrigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.15"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </MultiDataTrigger.ExitActions>
                    </MultiDataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Direction="270" Opacity="0.5"/>
        </Border.Effect>
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="シェルの名前変更" Click="MenuItem_RenameShelf_Click"/>
                <Separator/>
                <MenuItem Header="折りたたみ" IsCheckable="True" IsChecked="{Binding IsCollapsed}" />
                <MenuItem Header="ゴーストモード" IsCheckable="True" IsChecked="{Binding IsGhostModeEnabled}" />
                <Separator/>
                <MenuItem Header="並べ替え">
                    <MenuItem Header="なし" Tag="None" Click="MenuItem_Sort_Click"/>
                    <MenuItem Header="名前順" Tag="Name" Click="MenuItem_Sort_Click"/>
                    <MenuItem Header="更新日時順" Tag="DateModified" Click="MenuItem_Sort_Click"/>
                    <MenuItem Header="種類順" Tag="Type" Click="MenuItem_Sort_Click"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="カラー変更">
                    <MenuItem Header="デフォルト" Tag="#CC1E1E24" Click="MenuItem_Color_Click"/>
                    <MenuItem Header="ブルー" Tag="#CC004488" Click="MenuItem_Color_Click"/>
                    <MenuItem Header="レッド" Tag="#CC880000" Click="MenuItem_Color_Click"/>
                    <MenuItem Header="グリーン" Tag="#CC006622" Click="MenuItem_Color_Click"/>
                    <MenuItem Header="オレンジ" Tag="#CCAA4400" Click="MenuItem_Color_Click"/>
                    <MenuItem Header="パープル" Tag="#CC440088" Click="MenuItem_Color_Click"/>
                    <Separator/>
                    <MenuItem Header="カスタム..." Click="MenuItem_CustomColor_Click"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="フォルダをリンク (スマートシェルフ)..." Click="MenuItem_SmartShelf_Click"/>
                <Separator/>
                <MenuItem Header="グリッドにスナップ" IsCheckable="True" x:Name="MenuSnapToGrid" Click="MenuItem_SnapToGrid_Click" Loaded="MenuItem_SnapToGrid_Loaded"/>
                <Separator/>
                <MenuItem Header="シェルを削除" Click="MenuItem_DeleteShelf_Click"/>
            </ContextMenu>
        </Border.ContextMenu>
        <Border.RenderTransform>
            <TranslateTransform Y="0"/>
        </Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.0" To="1.0" Duration="0:0:0.4"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.4">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border CornerRadius="11,11,0,0" Padding="10,6">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#66000000" Offset="0"/>
                        <GradientStop Color="#00000000" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                 <Border.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" Command="{x:Static local:ShelfControl.RenameShelfCommand}" CommandParameter="{Binding}" />
                </Border.InputBindings>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Settings Button -->
                    <Button Grid.Column="1" 
                            Style="{StaticResource ChromeButtonStyle}"
                            Click="SettingsButton_Click"
                            ToolTip="設定"
                            Margin="4,0,0,0"
                            Visibility="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                         <Path Data="M12,15.5A3.5,3.5 0 0,1 8.5,12A3.5,3.5 0 0,1 12,8.5A3.5,3.5 0 0,1 15.5,12A3.5,3.5 0 0,1 12,15.5M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.35 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.96 19.05,5.05L16.56,6.05C16.04,5.66 15.5,5.32 14.87,5.07L14.5,2.42C14.46,2.18 14.25,2 14,2H10C9.75,2 9.54,2.18 9.5,2.42L9.13,5.07C8.5,5.32 7.96,5.66 7.44,6.05L4.95,5.05C4.73,4.96 4.46,5.05 4.34,5.27L2.34,8.73C2.21,8.95 2.27,9.22 2.46,9.37L4.57,11C4.53,11.35 4.5,11.67 4.5,12C4.5,12.33 4.53,12.65 4.57,12.97L2.46,14.63C2.27,14.78 2.21,15.05 2.34,15.27L4.34,18.73C4.46,18.95 4.73,19.04 4.95,18.95L7.44,17.95C7.96,18.34 8.5,18.68 9.13,18.93L9.5,21.58C9.54,21.82 9.75,22 10,22H14C14.25,22 14.46,21.82 14.5,21.58L14.87,18.93C15.5,18.68 16.04,18.34 16.56,17.95L19.05,18.95C19.27,19.04 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z" 
                               Fill="White" Stretch="Uniform" Margin="4"/>
                        <!-- Note: Click triggers color menu for now. Ideally should open full settings. -->
                         <Button.ContextMenu>
                             <ContextMenu>
                                <MenuItem Header="カラー変更">
                                    <MenuItem Header="デフォルト" Tag="#CC1E1E24" Click="MenuItem_Color_Click"/>
                                    <MenuItem Header="ブルー" Tag="#CC004488" Click="MenuItem_Color_Click"/>
                                    <MenuItem Header="レッド" Tag="#CC880000" Click="MenuItem_Color_Click"/>
                                    <MenuItem Header="グリーン" Tag="#CC006622" Click="MenuItem_Color_Click"/>
                                    <MenuItem Header="オレンジ" Tag="#CCAA4400" Click="MenuItem_Color_Click"/>
                                    <MenuItem Header="パープル" Tag="#CC440088" Click="MenuItem_Color_Click"/>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="フォルダをリンク (スマートシェルフ)..." Click="MenuItem_SmartShelf_Click"/>
                                <Separator/>
                                <MenuItem Header="グリッドにスナップ" IsCheckable="True" Click="MenuItem_SnapToGrid_Click" Loaded="MenuItem_SnapToGrid_Loaded"/>
                             </ContextMenu>
                         </Button.ContextMenu>
                         <!-- ContextMenu is opened via code-behind Click event -->
                    </Button>

                    <TextBlock Grid.Column="0" Text="{Binding Title}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold" 
                               VerticalAlignment="Center" FontSize="14">
                         <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRenaming}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="14" FontWeight="SemiBold">
                        <i:Interaction.Behaviors>
                            <behaviors:InlineRenameBehavior OnCompleteCommand="{Binding EndRenameCommand}"/>
                        </i:Interaction.Behaviors>
                         <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRenaming}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
            </Border>
            
            <!-- Items Area (Collapsible) -->
            <ListBox x:Name="ItemsListBox" Grid.Row="1" ItemsSource="{Binding Items}" 
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.Style>
                    <Style TargetType="ListBox">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCollapsed}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.Style>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="70" Height="86" Margin="4">
                            <Grid Width="48" Height="48" Margin="0,4,0,4">
                                <Image Source="{Binding Icon}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Grid>
                            <Grid>
                                <TextBlock Text="{Binding Title}" Foreground="{StaticResource TextSecondaryBrush}" 
                                           FontSize="11" TextWrapping="Wrap" TextAlignment="Center"
                                           TextTrimming="CharacterEllipsis" MaxHeight="30" LineHeight="14">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRenaming}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                         FontSize="11" TextAlignment="Center" TextWrapping="Wrap">
                                    <i:Interaction.Behaviors>
                                        <behaviors:InlineRenameBehavior OnCompleteCommand="{Binding EndRenameCommand}"/>
                                    </i:Interaction.Behaviors>
                                     <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRenaming}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                            </Grid>


                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Edit Chrome (Visible only in EditMode) -->
            <!-- Resize Handle -->
            <Thumb Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Cursor="SizeNWSE" Width="20" Height="20"
                   DragDelta="ResizeHandle_DragDelta"
                   Visibility="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Thumb.Template>
                    <ControlTemplate>
                        <Grid Background="Transparent">
                             <Path Data="M16,16V4H18V16H6V18H16V16Z" Fill="{StaticResource TextSecondaryBrush}" Opacity="0.5" 
                                   HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="2"/>
                        </Grid>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            
            <!-- Delete Button -->
            <Button Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0, -10, -10, 0"
                    Style="{StaticResource ChromeButtonStyle}"
                    Background="#CC880000"
                    Click="MenuItem_DeleteShelf_Click"
                    Visibility="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                      Fill="White" Stretch="Uniform" Margin="4"/>
            </Button>
        </Grid>
    </Border>
</UserControl>
